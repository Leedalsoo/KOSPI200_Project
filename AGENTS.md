# AGENTS.md — KOSPI200 옵션 자동매매 시스템 로컬 작업 지침

이 문서는 이 저장소에서 작업하는 모든 AI 에이전트(Antigravity, Claude Code 등)가
**작업을 시작하기 전에 반드시 읽어야 하는 최상위 작업 기준 지침**이다. 저장소의 설계 및 도메인 원칙을
자족적으로(self-contained) 담고 있으며, 프로젝트 소유자가 지정한 작업 연속성, Notion 기준점(지시프롬프트, 질문과답변/Q&A_Antigravity),
AI 작업지시 절차 및 검증·Git 반영 규칙을 통합하여 규정한다.

---

## 0. 프로젝트 개요

코스피200 옵션/선물을 대상으로 하는 다중 전략 자동매매 시스템이다. 아래 4개의 실행
환경(Environment)을 갈아끼울 수 있는 하나의 표준 코어(Standard Option Core)를
목표로 재설계되었다:

1. **High-Speed Test** — 초고속 합성 데이터로 로직만 빠르게 검증
2. **Virtual Trading** — 1배속 실시간 가상시장 + 가상증권사
3. **Paper Trading** — 실제 증권사(한국투자증권 KIS Developers)의 모의투자 API 사용
4. **Live Trading** — 실제 증권사 실계좌 API 사용

이 4개 환경은 Market Data, Clock, Broker, Account, Execution, Runtime Policy를
포함한 "Environment Bundle" 전체를 교체하는 방식으로 전환되며, **Strategy와 Core는
어떤 환경에서 실행되는지 알지 못한다** (특정 API를 직접 호출하지 않는다).

---

## 1. 절대 원칙 (모든 작업에 예외 없이 적용)

1. **가짜 성공을 성공으로 포장하지 않는다.** Mock/합성(synthetic) 데이터로 통과한
   테스트를, 실제 외부 시스템(KIS API, 실제 거래소 데이터 등)에 대한 검증 성공인
   것처럼 표현하지 않는다.
2. **모르면 만들지 않는다.** 종목 식별자, 계약 필드, 외부 API 응답 형식 등 공식
   근거가 없는 값은 임의로 추정/생성하지 말고, 코드에 `BLOCKED` 또는 `NotImplemented`로
   명시하고 그 이유를 남긴다. "일단 그럴듯하게 채워 넣고 나중에 고친다"는 방식을 쓰지 않는다.
3. **PASS 선언을 그대로 믿지 않는다.** 어떤 에이전트가 "완료/PASS"라고 보고해도,
   실제 코드 변경분과 테스트 실행 로그(실행 명령, 결과, exit code)를 직접 대조해서
   확인하기 전까지는 완료로 간주하지 않는다.
4. **가상 환경의 데이터/계약은 실제 외부 시스템 규격을 그대로 따른다.**
   - 가상증권사(Virtual Broker Adapter)는 실제 한국투자증권 KIS Developers API의
     요청/응답 필드, TR ID 구조, 인증 방식을 그대로 미러링한다.
   - 가상거래소(Virtual Market/시세 생성기)는 실제 한국거래소(KRX) 시세 API
     응답 규격(필드명, 단위, 갱신주기)을 그대로 따른다.
   - 이렇게 해야 Virtual → Paper → Live 전환 시 Adapter 내부의 실제 인증/네트워크만
     교체될 뿐, Core/Strategy/Contract 구조는 전혀 바뀌지 않는다. 가상 데이터가
     실제 KIS/KRX 규격과 다르면 이 원칙이 깨진 것으로 간주하고 즉시 수정한다.
5. **이전 단계가 실제 실행으로 검증되기 전까지 다음 단계로 넘어가지 않는다.**
   설계/구조 논의만으로 "완료"라고 하지 않는다.
6. **기능 검증이 끝나기 전에는 성능/가독성 리팩토링을 하지 않는다.** 리팩토링은
   해당 기능이 통합 테스트까지 통과한 뒤에만 진행한다.

---

## 2. 확정된 사업 규칙 (Domain Definitions)

아래는 프로젝트 소유자가 명시적으로 확정한 사업 규칙이다. 코드 구현 시 이 값을
authoritative source로 삼는다. 여기 없는 세부사항은 임의로 채우지 말고 질문한다.

### 2.1 Track1 — 테일 방어 가두리 전략 (선물헷지 정책)
- 헷지 진입 트리거: 매도 옵션 행사가 접근 90% 지점
- 헷지 상품: **코스피200 미니선물** (정규선물의 1/5 배수, 1포인트당 5만원)
- 헷지 수량 산식: `헷지 미니선물 수량 = ceil(매도 옵션 순델타 합계 × 5)`
- 헷지 해제(청산) 조건: 1.5포인트 반전
- 일일 최대 헷지 횟수: 20회 (휩소whipsaw 방지 목적)
- 사용 월물: 최근월물, 만기 D-1에 차근월물로 롤오버
- (2026-09 시점, 위 헷지 구조 외의 상세 상태 전이 — 가두리 형성/순환/청산 전체 로직은
  **재정의 진행 중**이며 별도 세션에서 실제 숫자 시나리오 기반으로 확정한다. 아래
  "3. 진행 중인 재작업" 참조.)

### 2.2 Track4 — 감마 스캘핑 (qty/side 의미 및 헷지 정책)
- qty의 의미: 델타 익스포저 (계약 수가 아니라 상쇄해야 할 순델타 크기)
- side의 의미: 신규 방향성 베팅이 아니라, 기존 옵션 포지션의 델타를 0으로
  되돌리기 위한 반대매매
- 재조정(리밸런싱) 트리거: 순델타 ±0.1 이탈 시
- 헷지 상품/수량 산식: Track1과 동일 — 코스피200 미니선물, `ceil(상쇄 필요 순델타 × 5)`
  (Track1과 공통 유틸 함수로 공유 가능: `delta_to_mini_futures_qty()`)

### 2.3 종목 식별자 / 선물·옵션 종목코드 매핑
- Authoritative source: 한국투자증권 KIS Developers 공식 종목정보 마스터파일
  (`.mst` 파일, 예: `fo_idx_code_mts.mst` — 국내선물옵션 지수상품)
- 공급처: KIS Developers API 문서 내 "종목 다운로드" 섹션. **로그인 불필요**
  (KIS 공식 안내: API 문서/공지/테스트베드 열람은 로그인이 불필요한 작업), 매일 다수 회 갱신
- 파일 인코딩: EUC-KR (한글 종목명 필드만 해당, 코드 필드는 ASCII)
- 필드 계약 (파이프 `|` 구분, 9개 필드):
  1. 시장구분코드 (예: `1`=코스피200선물, `B`=코스피200미니선물, `3`=코스닥150선물,
     `H`=KRX300선물, `7`/`8`=VKOSPI 선물/스프레드, `2`=캘린더스프레드,
     `O`/`S`=옵션(정규/미니/위클리))
  2. 단축코드(shrn_iscd) — **실제 주문 API 호출 시 사용하는 종목코드**
     (예: `A01609`, `CAFBXW990`)
  3. 표준코드 (ISIN 유사 12자리)
  4. 종목명+상품유형(F/P/C)+만기월(YYYYMM) 또는 옵션 행사가/주차 정보
  5. (옵션 전용) 콜/풋 구분 또는 공백
  6. 행사가/기준가 (선물은 대부분 0)
  7. 근월물 기준 순번 (1=최근월물)
  8. 기초자산코드 (예: `2001`=KOSPI200, `0503`=VKOSPI, `3003`=KSQ150)
  9. 기초자산명
- 이 마스터파일이 유일한 authoritative source다. 종목코드를 임의로 조합/추정해서
  생성하지 않는다.
- 이 파일을 정적으로 하드코딩하지 말고, 매일 갱신되는 특성을 반영해 주기적
  갱신/캐시 정책을 갖는 `InstrumentMasterProvider` 형태의 어댑터로 감쌀 것.

---

## 3. 알려진 코드 결함 (로컬 통합 이전에 발견됨, 재확인 필요)

과거 버전(리팩토링 이전 코드)을 분석하며 발견된 결함 목록이다. 새 구조(Standard
Core 기반 재작성)에서 동일 결함이 있는지 반드시 재확인하고, 있다면 수정한다.

| 우선순위 | 위치 | 내용 |
|---|---|---|
| 최고 | 주문 FSM ↔ 비상정지(Panic Halt) | 비상정지가 주문을 `STANDBY_OVERRIDE`라는, FSM 전이표에 존재하지 않는 상태로 보내려다 실패하고, 그 다음 `CANCELLED`로도 직접 전이가 안 되는 상태가 많아 **비상정지 버튼을 눌러도 실제로 주문이 취소되지 않을 위험**이 있었음. 전이 결과를 확인하지 않고 넘어가는 코드였음. 반드시 재확인. |
| 높음 | 리스크 게이트 | `allow_reduction`(한도 초과 시 전량거부 대신 가능한 만큼만 축소 승인) 파라미터가 실행 엔진에서 항상 기본값(False)으로 호출되어, 한도를 살짝만 넘어도 무조건 전량 거부되던 문제. |
| 중간 | 신호 생성기 | 중복신호 제거(dedup) 로직이 구현은 되어 있으나 실제 파이프라인에서 호출되지 않던 문제. |
| 중간 | 신호 중재기 | 선물(FUTURES) 주문은 행사가/옵션타입이 없어 종목 식별키가 동일해지므로, 서로 다른 전략의 선물 주문이 의도치 않게 충돌 처리될 수 있는 구조. |
| 중간 | 여러 전략 파일 | 함수는 정의되어 있으나 실제 실행 경로에서 호출되지 않는 죽은 코드 다수 (예: 나비/캘린더 스프레드 함수, 세타가드 함수, 매크로 국면 보호 함수, 오전 청산 함수 등). |
| 낮음 | 일부 전략 파일 | 동일 로직의 async/sync 버전이 중복 구현되어 있고, 실제로는 한쪽만 호출됨. |
| 낮음 | orchestrator 관련 구 파일 | 국면별 자본 가중치 배분표가 일부 전략(Track5~9)을 아예 누락한 채 방치되어 있었고, 실제 실행 엔진은 이 파일을 아예 참조하지 않는 고아 코드였음. |

---

## 4. 여전히 남아있는 미해결 항목 (SOURCE BLOCKED — 로컬 실행 전에는 풀리지 않음)

아래 두 항목은 데이터를 찾아서 푸는 문제가 아니라, **실제로 프로세스가 살아있는
상태에서만 자연스럽게 채워지는** 항목이다. 실제 caller/이벤트 공급자가 없는 상태에서
임의로 값을 만들어 채우지 않는다.

1. **주문 명령(BrokerOrderCommand)의 실제 생성 주체(production caller)** — 신호→중재→
   리스크 게이트를 통과한 주문이 실제로 누구에 의해 최종 조립되어 브로커에 전달되는지,
   실제 실행 프로세스가 기동되어야 확정된다.
2. **체결 이벤트의 실제 공급원** — 특히 통계적 차익 전략(Track3) 등에서 사용하는
   체결 이벤트가 실시간 웹소켓 체결통보(KIS `H0IFCNI0`)와 REST 복구 조회 중
   어느 경로로, 어떤 공통 식별자로 들어오는지는 실제 연결이 있어야 확정된다.

---

## 5. 로컬 작업 단계 (Local Phase 1~12)

이전 단계가 **실제 실행 기준으로** 검증되기 전에는 다음 단계에 착수하지 않는다.
각 Phase 착수 전, 프로젝트 소유자에게 이 문서를 기준으로 확인을 받는다.

1. **통합 실행검증** (최우선, 즉시 착수)
   - 종목 마스터파일(`fo_idx_code_mts.mst`)을 실제 `InstrumentMasterProvider`에 연결
   - 코스피200 미니선물(시장구분 `B`) 레코드를 Track1/Track4 헷지 대상과 대조 연결
   - Virtual/High-Speed/Paper/Live 4개 Environment Bundle 전체를 하나로 묶어 실제 실행
   - 완료 조건: 최소 1개 전략 기준으로 신호→중재→리스크→주문→체결→포지션→PnL
     전체가 실제로 한 번은 끝까지 돈다
2. **이중 AI 교차검증 체계 가동**
   - 1차: Antigravity가 실행검증/수정 수행 (별도 Git 브랜치)
   - 2차: Claude가 동일 작업을 독립적으로 재검증 (별도 브랜치)
   - 결과가 다르면 자동으로 승자를 정하지 않고, diff를 사람에게 보여주고 선택받는다
   - 안전 직결 항목(킬스위치, 마진, 중복주문 방지 등)만 전량 이중검증하고, 단순
     리팩토링은 단일 검증으로 충분
3. **전략 보강 및 재작성** — Track1(선물헷지 정책 확정, 나머지 상태 전이는 재정의 진행 중),
   Track2 포함 9개 전략을 프로젝트 소유자의 실제 의도와 대조하여 부족한 부분을 채우거나
   필요 시 처음부터 재작성한다. 신규 전략 추가 여부도 이 단계에서 검토한다.
4. 센서(Sensor) 계층 고도화
5. 리스크(Risk) 계층 고도화 — 위 3절의 알려진 결함(PANIC HALT FSM, REDUCE 미연결)
   재확인 및 수정 포함
6. 코드 효율화/정리 (죽은 코드, 중복 구현 정리 — 기능 검증 완료 후에만)
7. UI/Control Tower 완성 (위급상황 통제 탭 포함)
8. 서킷브레이커/예외처리 정밀화 (실제 장기 모의투자 운영에서 관찰된 사례 기반으로만)
9. 외부 서버 배포 + 보안 (접근제어/키관리/네트워크 보안)
10. 이중화 및 자동복구 (백업 서버, 자동 failover)
11. 실거래(REAL) 소액 진입 — 명시적 ARM, 최소 주문규모, 점진적 확대
12. 다중 증권사 확장 — 최소 1개 증권사(KIS)로 충분히 안정화된 이후에만 착수

---

## 6. 전략 로직을 정의/수정할 때의 작업 방식

전략 로직(특히 진입/헷지/청산 조건)을 프로젝트 소유자와 논의할 때는, 추상적인
설명이나 AI가 먼저 제시하는 구조화된 틀(예: 상태 목록)에 맞춰달라고 요청하지 않는다.
대신:
1. 프로젝트 소유자가 순서 상관없이 자유롭게 생각을 던지도록 한다 (중복/모순 허용)
2. 그 내용을 AI가 받아서 조건→숫자→행동이 명확한 구체적 시나리오(실제 지수 값,
   실제 행사가, 실제 트리거 조건)로 재구성한다
3. 재구성한 결과를 다시 보여주고 소유자가 직접 틀린 부분을 지적하게 한다
4. 이 과정을 반복해서 애매한 지점을 전부 구체적 숫자/조건으로 확정한 뒤에만 코드로 옮긴다

애매한 부분을 AI가 "그럴듯하게" 임의로 채워서 구현하지 않는다 — 이것이 과거 여러 차례
재작업이 반복된 핵심 원인이었다.

---

## 7. 작업 연속성 및 Notion 기준 절차 (Work Continuity & Notion Standards)

프로젝트 소유자가 지정한 작업 연속성, Notion 기준점, AI 작업지시 절차, 코드 검증 및 Git 반영 절차를 명시한다. 이 절차는 AI의 기억 소실 방지 및 코드 작업 간 엉킴을 원천 차단하기 위한 필수 규칙이다.

### 7.1 작업 시작 전 필수 확인
1. **원격 Git Project200의 최상위 AGENTS.md 전체를 가장 먼저 읽는다.**
2. AGENTS.md를 이 저장소의 최상위 작업 기준으로 적용한다.
3. 작업에 관련된 경우 Notion의 **질문과답변**을 단순 기록이 아니라 작업의 연속성을 유지하는 기준점으로 확인한다.
4. **Q&A_Antigravity**의 기존 관련 기록도 확인하여 이전 작업과 현재 작업이 엉키지 않도록 한다.
5. Notion 질문과답변은 대화의 영속성 및 AI의 기억 소실과 코드 작업 간 엉킴을 방지하기 위한 기준 자료로 취급한다.
6. 특정 Notion 페이지가 작업지시에서 지정된 경우 해당 페이지를 우선 읽고 그 내용을 현재 작업의 기준으로 삼는다.

### 7.2 작업 처리 절차
사용자의 질문/지시에 따라 작업 또는 검토가 필요한 경우 다음 절차를 따른다.
1. 사용자가 지정한 Notion 페이지가 있으면 먼저 읽는다. 지정 페이지가 없으면 사용자의 현재 질문/지시를 기준으로 판단한다.
2. 실제 코드 작업이 필요한 경우 AI 작업용 프롬프트를 작성한다.
3. 작성된 프롬프트는 Notion **지시프롬프트** 하위에 `No. *** 프롬프트제목` 형식의 페이지로 기록한다.
4. 작업 결과 또는 답변 요약은 Notion **질문과답변** (또는 `Q&A_Antigravity`) 하위에 `[No.xxx 답변내용요약]` 형식의 페이지로 기록한다.
5. 위 기록은 단순한 이력 보관이 아니라 이후 작업에서 현재 상태와 이전 판단을 연결하는 기준으로 사용한다.
6. AI가 작업을 정확하게 완료하고 검증까지 PASS한 경우에만 원격 Git `Project200` 브랜치에 commit 및 push한다.

### 7.3 코드 검증 및 임시 Python workspace 운영 원칙
1. 코드 작업은 반드시 실제 저장소의 현재 상태를 기준으로 수행한다.
2. 필요하면 Git에서 임시 Python workspace를 구성하여 관련 코드와 테스트를 함께 실행하고 검증한다.
3. 임시 workspace는 실제 프로젝트 구조와 계약을 확인하기 위한 검증 목적으로만 사용하며, 검증 결과를 실제 코드의 근거 없이 추정해서는 안 된다.
4. 테스트 결과는 실행 명령, 결과, exit code를 확인하여 보고한다.
5. 실제 외부 시스템과 연결되지 않은 상태에서 mock/synthetic 검증을 수행했다면 이를 실제 외부 시스템 검증 성공으로 표현하지 않는다 (절대 원칙 1 준수).
6. 알 수 없는 값이나 확인되지 않은 외부 계약은 임의로 채우지 말고 기존 AGENTS.md의 BLOCKED/NotImplemented 원칙을 따른다 (절대 원칙 2 준수).

### 7.4 Notion 작업 권한 및 기록 원칙
1. 이 작업환경에서 Notion 연결/권한이 제공되는 경우 AI는 필요한 Notion 페이지를 생성·수정할 수 있다.
2. 사용자가 지정한 Notion 구조를 임의로 변경하지 않는다.
3. 질문과답변의 기존 기록은 삭제하거나 덮어쓰지 않고, 새로운 작업은 새로운 `[No.xxx 답변내용요약]` 페이지로 연결한다.
4. 기존 지시프롬프트 기록도 임의로 삭제하지 않고 새로운 작업은 새로운 `No. *** 프롬프트제목` 페이지로 기록한다.
5. Notion에 접근할 수 없는 환경에서는 접근하지 못했다는 사실을 명확히 보고하고, 없는 기록을 만들어냈다고 주장하지 않는다.

### 7.5 Git commit / push 필수 조건 및 규칙
1. 작업을 정확하게 완료하고 검증 기준을 모두 통과한 경우에만 commit/push한다.
2. 변경사항이 실제로 존재하는지 `git status` 및 `git diff`로 먼저 확인한다.
3. 임시 파일, 캐시, secrets, `.env` 등의 민감정보가 commit에 포함되지 않았는지 철저히 확인한다.
4. 실제 변경사항이 없으면 빈 commit을 만들지 않는다.
5. 테스트가 FAIL, BLOCKED 또는 검증 결과가 불확실하면 commit/push하지 않는다.
6. push가 실패하면 성공한 것처럼 보고하지 말고 `PUSH BLOCKED — permission/authentication limitation` 등 실제 원인을 명시한다.
7. push가 성공한 경우 원격 `Project200`의 HEAD가 방금 push한 commit을 가리키는지 확인한다.
8. 최종 보고에는 변경 파일, 테스트 명령/결과/exit code, 검증 상태, commit SHA, push 결과, 원격 HEAD를 반드시 포함한다.
